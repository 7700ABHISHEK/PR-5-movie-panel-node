<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= movie ? movie.name : "Movie Details" %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #121212;
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .hero {
            position: relative;
            width: 100%;
            height: 70vh;
            overflow: hidden;
            border-radius: 12px;
            margin-bottom: 30px;
        }

        .hero img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: brightness(50%);
        }

        .hero-text {
            position: absolute;
            bottom: 20px;
            left: 30px;
            color: #fff;
        }

        .hero-text h1 {
            font-size: 3rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .badge-genre {
            font-size: 0.9rem;
            margin-right: 5px;
        }

        .rating {
            font-size: 1.2rem;
            color: #ffcc00;
        }

        .description {
            font-size: 1.1rem;
            line-height: 1.6;
        }

        a.btn-back {
            margin-top: 20px;
        }

        @media (max-width: 768px) {
            .hero-text h1 {
                font-size: 2rem;
            }
        }
    </style>
</head>

<body>

    <div class="container mt-4">
        <% if (!movie) { %>
            <div class="text-center text-muted mt-5">
                <h3>Movie not found</h3>
                <a href="/" class="btn btn-primary mt-3">Back to Movies</a>
            </div>
            <% } else { %>
                <div class="hero">
                    <img src="/<%= movie.photo %>" alt="<%= movie.name %>">
                    <div class="hero-text">
                        <h1>
                            <%= movie.name %>
                        </h1>
                        <div>
                            <span class="badge bg-primary badge-genre">
                                <%= movie.genre %>
                            </span>
                            <span class="rating"><i class="bi bi-star-fill"></i>
                                <%= movie.rating %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-lg-8 mx-auto">
                        <h3>Description</h3>
                        <p class="description">
                            <%= movie.description %>
                        </p>
                        <a href="/" class="btn btn-outline-light btn-back"><i class="bi bi-arrow-left"></i> Back to
                            Movies</a>
                    </div>
                </div>
                <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>